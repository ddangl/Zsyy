# 打卡数据整理助手 - 云端部署改造方案

## 项目背景

将现有的 Windows 桌面应用（tkinter + EXE）改造为云端 Web 应用，让任何人通过浏览器即可使用。

## 架构对比

| | 桌面版（现有） | 云端版（目标） |
|---|---|---|
| UI | tkinter | HTML + JS（浏览器） |
| 后端 | 无（本地直接运行） | FastAPI（Python） |
| 文件 | 本地选择 | 上传到服务器临时目录 |
| 图片生成 | Playwright 本地截图 | 服务端 Playwright 无头截图 |
| 部署 | Windows EXE | Docker（任意 Linux/Windows） |

## 已完成的文件

### 新增文件

```
e:\打卡统计\上班统计\
├── Dockerfile              # Docker 镜像构建
├── docker-compose.yml      # Docker Compose 一键启动
├── requirements.txt        # Python 依赖（根目录，Docker 构建用）
└── app/
    ├── main.py             # FastAPI 后端 API
    ├── requirements.txt    # Python 依赖（app 目录备份）
    └── static/
        └── index.html      # Web 前端页面
```

### 复用的文件（无需修改）

- `processor.py` - 核心数据处理逻辑，完全复用
- `config.py` - 配置文件，完全复用

## API 接口说明

### 1. 上传文件
```
POST /api/upload
Content-Type: multipart/form-data
参数: file (Excel 文件)
返回: { "file_id": "xxx", "filename": "xxx.xlsx" }
```

### 2. 处理文件
```
POST /api/process?file_id=xxx&show_missing=true
返回: { "logs": "处理日志", "image_url": "/api/image/xxx/xxx.png", "error": null }
```

### 3. 获取表格概览（用于修改表格）
```
GET /api/overview/{file_id}
返回: { "headers": [...], "rows": [...], "columns": [...] }
```

### 4. 保存修改
```
POST /api/save/{file_id}
Body: { "changes": { "行idx": { "列名": "新值" } }, "new_rows": [...] }
返回: { "message": "已保存 N 条修改", "saved": N }
```

### 5. 获取图片
```
GET /api/image/{file_id}/{image_name}
返回: PNG 图片文件
```

## 前端功能

Web 前端完全复刻了 tkinter UI 的功能：

1. **文件选择** - 点击按钮上传 Excel 文件
2. **开始处理** - 调用 API 处理文件，显示日志，展示结果图片
3. **修改表格** - 弹窗编辑表格，双击单元格编辑，支持搜索/新增/保存
4. **隐藏未打卡房间** - 复选框选项
5. **下载图片** - 处理完成后可下载生成的 PNG

## 部署方式

### 方式一：Docker Compose（推荐）

```bash
# 构建并启动
docker-compose up -d --build

# 访问
http://localhost:45785
```

### 方式二：Docker 手动构建

```bash
docker build -t daka-web .
docker run -d -p 45785:45785 -v ./data:/app/app/uploads -v ./output:/app/app/outputs daka-web
```

### 方式三：本地直接运行

```bash
# 安装依赖
pip install -r requirements.txt

# 启动
cd e:\打卡统计\上班统计
uvicorn app.main:app --host 0.0.0.0 --port 45785
```

## Dockerfile 说明

```dockerfile
FROM python:3.12-slim
# 安装 Playwright Chromium 系统依赖
# 安装 Python 依赖 + Playwright Chromium
# 复制 processor.py, config.py, app/
# 启动 uvicorn
```

镜像包含：
- Python 3.12
- FastAPI + uvicorn
- pandas + openpyxl（Excel 处理）
- Playwright + Chromium（HTML 转图片）

## 宝塔 Linux 部署步骤

### 前提条件

- 宝塔面板已安装（推荐 7.x / 8.x / 9.x）
- 服务器至少 2GB 内存（Playwright Chromium 需要）
- 服务器可以访问外网（拉取 Docker 镜像和 pip 包）

### 第一步：安装 Docker 管理器

1. 登录宝塔面板
2. 左侧菜单 → **Docker** 或 **软件商店**
3. 搜索 **Docker管理器** → 安装
4. 安装完成后进入 Docker 管理器

### 第二步：上传项目文件

1. 左侧菜单 → **文件**
2. 进入 `/www/wwwroot/` 目录（或你喜欢的目录）
3. 新建文件夹 `daka-web`
4. 上传以下文件（保持目录结构）：

```
/www/wwwroot/daka-web/
├── Dockerfile
├── docker-compose.yml
├── requirements.txt
├── processor.py
├── config.py
└── app/
    ├── main.py
    └── static/
        └── index.html
```

> 可以直接把整个 `e:\打卡统计\上班统计` 目录打包成 zip 上传，然后解压

### 第三步：构建 Docker 镜像

**方法 A：通过宝塔终端（推荐）**

1. 左侧菜单 → **终端**
2. 执行：

```bash
cd /www/wwwroot/daka-web
docker-compose up -d --build
```

等待构建完成（首次约 5-10 分钟，下载 Python 镜像 + Chromium）。

**方法 B：通过宝塔 Docker 管理器**

1. Docker管理器 → **Compose** → **添加Compose项目**
2. 名称填 `daka-web`
3. 目录选择 `/www/wwwroot/daka-web`
4. 点击 **添加**

### 第四步：验证运行

1. 浏览器访问 `http://你的服务器IP:45785`
2. 看到"打卡数据整理助手"页面即部署成功

### 第五步：配置域名 + HTTPS（可选）

1. 左侧菜单 → **网站** → **添加站点**
2. 填入你的域名（如 `daka.example.com`）
3. 创建完成后 → 点击站点名 → **反向代理**
4. 添加反向代理：
   - 目标URL：`http://127.0.0.1:45785`
   - 发送域名：`$host`
5. 保存后，点击 **SSL** → 申请 Let's Encrypt 免费证书
6. 开启强制 HTTPS

最终通过 `https://daka.example.com` 访问。

### 第六步：防火墙放行

如果直接用 IP:端口 访问，需要在宝塔和云服务商都放行端口：

1. 宝塔面板 → **安全** → 放行 `45785` 端口
2. 云服务商控制台 → 安全组 → 入站规则 → 添加 `45785/TCP`

> 配置了反向代理域名后，可以关闭 45785 的外部访问，只保留 80/443

### 常用运维命令

```bash
# 查看运行状态
docker-compose ps

# 查看日志
docker-compose logs -f

# 重启
docker-compose restart

# 停止
docker-compose down

# 更新代码后重新构建
docker-compose up -d --build
```

### 故障排查

| 问题 | 解决方案 |
|---|---|
| 端口访问不了 | 检查防火墙/安全组是否放行 45785 |
| 构建失败（网络超时） | 换 pip 源：在 Dockerfile 的 pip install 后加 `-i https://pypi.tuna.tsinghua.edu.cn/simple` |
| Chromium 安装失败 | 确保内存 >= 2GB，或加 swap |
| 内存不足 | `docker-compose.yml` 加 `mem_limit: 2g` |
| 上传文件太大报错 | 修改 `app/main.py` 中的 `limit: '10mb'` |

---

## 未完成 / 待验证

1. **本地启动测试** - 需要安装 fastapi/uvicorn 依赖后测试
   ```
   pip install fastapi uvicorn python-multipart
   uvicorn app.main:app --host 0.0.0.0 --port 45785
   ```

2. **Docker 构建测试** - 在有 Docker 的环境中执行 `docker-compose up --build`

3. **大文件上传** - FastAPI 默认限制可能需要调整（当前设置了 10MB）

4. **清理策略** - uploads/outputs 目录需要定期清理临时文件

5. **生产部署建议**：
   - 加 Nginx 反向代理
   - 加 HTTPS（Let's Encrypt）
   - 加文件大小限制
   - 加定期清理 cron job

## 核心代码逻辑说明

### app/main.py 关键设计

- `sys.path.insert(0, 上级目录)` 让 app/ 内能 import 上层的 processor 和 config
- 上传文件按 `file_id` 分目录存储，避免并发冲突
- `process_file` 使用 `redirect_stdout` 捕获 processor.py 的 print 输出作为日志
- 处理时 `os.chdir(output_dir)` 确保生成的图片输出到正确目录
- `save_changes` 复用 `save_excel_overview` 和 `patch_excel_data`，逻辑完全一致

### app/static/index.html 关键设计

- 纯 HTML + CSS + 原生 JS，无框架依赖
- 修改表格用弹窗 + 双击编辑实现，与 tkinter 版行为一致
- `modifiedCells` 字典跟踪修改，`addedRows` 数组跟踪新增行
- 保存时发送到 `/api/save/{file_id}`，后端直接调用 processor 保存
